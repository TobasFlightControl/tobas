# Do not delete or rename this file because it is executed in tobas_f550_np_config/gazebo.launch.xml.

from launch import LaunchDescription


def generate_launch_description():
    ld = LaunchDescription()

    # Please add the nodes that run only during the Gazebo simulation.

    return ld
