# Tobas Bootmedia Config

## Run as root

```bash
$ cd ~/colcon_ws
$ sudo env XDG_CONFIG_HOME="$HOME/.config" bash -c 'source ./install/setup.bash && ros2 run tobas_bootmedia_config TobasBootmediaConfig'
```
