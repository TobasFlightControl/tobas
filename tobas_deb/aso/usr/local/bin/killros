#!/bin/bash

for pid in $(ps aux | grep ros | grep -v grep | grep -v $$ | awk '{print $2}'); do
  if [ -e /proc/$pid ]; then
    echo "Killing process $pid"
    kill -9 $pid
  else
    echo "Process $pid does not exist, skipping"
  fi
done
